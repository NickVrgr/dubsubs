<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Used only for the optional mic-assisted sync feature (voice-activity
         detection to nudge subtitle timing). Audio is analyzed in memory in
         real time and never recorded to disk or transmitted. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application
        android:label="dubsubs"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Lets DubSubs be offered as an "Open with" target for .srt files.
                 Android has no registered MIME type for .srt, so file managers send
                 wildly different (or no) mimeType values — match on the extension via
                 pathPattern instead, with mimeType left wildcarded. The pathPattern is
                 repeated because Android's glob matcher only expands "*" against the
                 single preceding character, so a name with more than one "." (e.g.
                 "movie.en.srt") needs one extra ".*" per extra dot to match. -->
            <intent-filter android:label="Open with DubSubs">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:mimeType="*/*"/>
                <data android:host="*"/>
                <data android:pathPattern=".*\\.srt"/>
                <data android:pathPattern=".*\\..*\\.srt"/>
                <data android:pathPattern=".*\\..*\\..*\\.srt"/>
            </intent-filter>
            <!-- Lets DubSubs be offered as a target in the system Share sheet
                 (as opposed to "Open with", above). Android's SEND intent has
                 no filename to pattern-match against — only a declared MIME
                 type — and since .srt has no MIME type registered on Android,
                 senders pick whatever they feel like (often text/plain or
                 application/octet-stream). List every type we've seen used in
                 practice; MainActivity.kt still checks the actual filename
                 before treating the shared content as a subtitle file. -->
            <intent-filter android:label="Share with DubSubs">
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
                <data android:mimeType="application/x-subrip"/>
                <data android:mimeType="text/srt"/>
                <data android:mimeType="application/octet-stream"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
